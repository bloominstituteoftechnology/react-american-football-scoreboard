import React from 'react';

function QuarterButton(props) {
  return (
    <div>
      <button className="homeButtons__touchdown" onClick={props.quarty}>Quarter</button>
    </div>
  )
}

export default QuarterButton;
